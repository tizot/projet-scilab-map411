\documentclass[12pt]{scrartcl}

\KOMAoptions{paper=a4, DIV=14}

% Extension de capacités
\usepackage{etex}

% Francisation du document
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Police de caractères
\usepackage{lmodern}
\usepackage{fourier-orns}

% Listes d'énumération
\usepackage{enumitem}

% Mathématiques
\usepackage{mathtools}
\usepackage{mathrsfs}
\usepackage{mathdots}

% Références croisées
\usepackage[french]{varioref}

% Couleurs et boites
\usepackage[x11names]{xcolor}
\usepackage{fancybox}

% Images
\usepackage{graphicx}

% Francisation du document, le retour
\usepackage[frenchb]{babel}

% Subfloat
\usepackage{caption}
\usepackage{subcaption}

% Date
\usepackage{datetime}
\newdate{date}{14}{01}{2015}

% Mes fonctions
\usepackage[ensemblesGras]{mesfonctions}

% Unités
\usepackage{siunitx}
\sisetup{%
  unitsep = \cdot,%
  decimalsymbol = comma,%
  expproduct = \cdot,%
  seperr,%
  trapambigerr = false,%
  alsoload = hep%
}

% Propriétés hypertexte
\usepackage[pdftex]{hyperref}
\hypersetup{%
  pdfauthor = {Bruneau Basile, Masset Camille},%
  pdftitle = {Un calcul de flot optique},%
  pdfsubject = {Projet de simulation - MAP411},%
  pdfkeywords = {},%
  pdfcreator = {pdfLaTeX},%
  pdfproducer = {pdfLaTeX},%
  bookmarksnumbered = true,%
  pdfstartview = FitH,%
  pdfpagelayout = OneColumn,%
  colorlinks = false,%
  pdfborder = {0 0 0}%
}

\title{Un calcul de flot optique}
\author{\textsc{Bruneau} Basile \and \textsc{Masset} Camille}
\date{\displaydate{date}}


\begin{document}
\setlist[itemize, 1]{label = \starredbullet}
\setlength{\parskip}{1em}

\maketitle

\input{parts/section1}
\input{parts/section2}
\input{parts/section3}




\end{document}